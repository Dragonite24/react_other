// import { createAction } from 'typesafe-actions'
import React from 'react'

// export const saga = createAction('api/authorization/createTempUser->SAGA')()
// export const start = createAction('api/authorization/createTempUser->START')()
// export const success = createAction('api/authorization/createTempUser->SUCCESS')<{ response: object }>()
// export const failure = createAction('api/authorization/createTempUser->FAILURE')<{ error: object }>()
